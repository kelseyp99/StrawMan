{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Attach to Expo app",
        "type": "expo",
        "request": "attach",
        "cwd": "${workspaceFolder}"
      }
    ]
  }
  